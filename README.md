# YT_ELT

#Create venv on my macOS
python3 -m venv venv

#Activate venv
source/venv/bin/activate

#Deactivate

# pip install requests

use pip list to check if it's installed, requests should appear on the list
